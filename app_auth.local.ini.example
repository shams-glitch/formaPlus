# Comptes applicatifs LOCAL — TEST ONLY
# Copier ce fichier vers app_auth.local.ini à côté de l'exe.
# Ne jamais le committer. Ce n'est PAS le mot de passe Oracle.
# Rôles autorisés : ADMIN (accès complet) ou FORMATEUR (consultation).

[admin]
username=
password=
role=ADMIN

[formateur]
username=
password=
role=FORMATEUR
